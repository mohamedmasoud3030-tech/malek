# Deterministic local/CI Supabase configuration.
# The database gate starts only PostgreSQL and excludes all non-database services.
project_id = "rentrix-ci"

[db]
port = 54322
shadow_port = 54320
major_version = 17

[db.migrations]
enabled = true
schema_paths = []

[db.seed]
enabled = false
sql_paths = []
