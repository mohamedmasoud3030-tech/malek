# 2026-08-19 Runtime Repair Record

This record supersedes the stale 2026-08-19 Auth/demo warnings currently present in `AGENT_HANDOFF.md`.

## Auth `admin/users` 500 — RESOLVED

Affected Auth user: `qa-agent-test@rentrix.local` (`84501d15-e68b-4475-b1d2-a654f73fc6f3`).

GoTrue failed to scan the legacy row because `confirmation_token` was `NULL` (`converting NULL to string is unsupported`). Direct comparison also found `recovery_token` and `email_change_token_new` as `NULL` while healthy rows store empty strings.

The database was explicitly designated experimental for this repair. The three malformed legacy token fields were normalized to `''` in place. The user was **not deleted**.

Post-repair verification:
- zero `auth.users` rows remain with NULL in those three fields;
- `custom_access_token_hook` resolves the main admin and QA user as `ADMIN` in company `00000000-0000-4000-8000-000000000001`;
- GitHub issue #1512 is closed as completed.

The old `AGENT_HANDOFF.md` instruction to delete this user and its `BLOCKED` status are obsolete and must not be followed.

## Demo data — ACCOUNTING-SAFE RESEED COMPLETED

The first demo seed inserted finance rows directly through REST. Those direct financial rows were removed and recreated through the canonical accounting RPCs after repairing the runtime RPC/schema drift.

Current demo module counts under the active company JWT:
- service providers: 3
- leads: 5
- maintenance records: 5
- expenses: 6 (five base demo expenses plus one expense generated by the governed deposit-deduction lifecycle)
- tenant deposits: 4 (three demo deposits plus one pre-existing deposit)

The five base demo expenses each have exactly two balanced journal lines. The three demo deposits cover held, partially deducted, and fully refunded states and include canonical contract/tenant/property/unit links, deposit transactions, and balanced journal movements.

## Financial RPC schema drift — RESOLVED

`journal_entries.id` is UUID, but four legacy RPC implementations attempted to insert `gen_random_uuid()::text`. Runtime posting failed with PostgreSQL `42804`.

Fixed functions:
- `create_expense_with_journal_atomic_phase3a1a_impl`
- `create_deposit_atomic`
- `deduct_deposit_atomic_phase3a1a_impl`
- `refund_deposit_atomic_phase3a1a_impl`

Expense display numbers were also changed from the wrapper-derived `EXP-YYYYMMDD-phase3` pattern to a generated expense UUID prefix.

## Active-company RLS — RESOLVED for demo modules

A negative JWT isolation test found that `expenses` and `tenant_deposits` could still be read when the same user supplied a different active-company claim because membership SELECT policies were permissive.

Restrictive `p0_tenant_isolation` policies were added to both tables.

Verification as role `authenticated`:
- real active-company claim: service providers 3, leads 5, maintenance 5, expenses 6, deposits 4;
- fake/foreign active-company claim: **0 / 0 / 0 / 0 / 0**.

No RLS policy was disabled and no application permission was widened.
