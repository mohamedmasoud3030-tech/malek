# Pilot demo seed test note

The seed safety contract is locked by `rentrix-app/scripts/pilot-demo-seed.test.mjs` using Node's built-in test runner. It verifies production refusal, explicit QA mutation approval, canonical financial RPC usage, and the promised demo scenario mix.
