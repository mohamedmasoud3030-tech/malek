# UI_UX_REMEDIATION_PLAN

> خطة تصحيح مرتبة حسب الأولوية، ضمن اتجاه التصميم الواحد «الوضوح الأخضر الموحّد».
> كل مرحلة (Milestone) صغيرة وآمنة وقابلة للعكس، تُفحص بعدها بالعرض الفعلي + الاختبارات + build + مراجعة Git diff.

## الأولوية

| الترتيب | البند | الحالة |
|---|---|---|
| P0 | توحيد لون البراند (بما فيه منع الوميض) | ✅ منفّذ (PR #1501 + هذه الجولة) |
| P0 | نصف قطر الكروت والحقول موحّد | ✅ منفّذ (#1502، #1503) |
| P1 | فعل الخطأ القانوني «تعذّر» | ✅ منفّذ (#1504) |
| P1 | كود CSS ميت | ✅ منفّذ (#1501) |
| P2 | صفحة التسويق للزائر الجديد (#6) | ⏳ قرار منتج — يحتاج موافقة |

## المراحل المتبقية (مقترحة، غير منفّذة)

### M1 — تقليل الكثافة (المرشح الأعلى قيمة)
- الهدف: خفض الضوضاء البصرية في الصفحات المزدحمة (Dashboard له 6 أقسام متراكمة).
- النهج: قياس `--dashboard-section-gap`، وتأخير الأقسام غير العاجلة خلف «عرض المزيد»، وتهذيب مسافات KPI.
- **يتطلب معاينة بصرية حية** لتحديد التكرار الصحيح (لا تخمين).

### M2 — صفحة تعريف للزائر الجديد
- البديلان: (أ) إعادة ربط landing بعد إصلاح أدائها، أو (ب) شريط تعريف مختصر داخل `/login`.
- قرار منتج: يحدد المالك أي مسار.

### M3 — تدقيق التباين الشامل (axe) عبر كل الطرق
- تشغيل فحص تباين آلي على الطرق الحرجة وتوثيق أي خرق AA.

## قاعدة التنفيذ

1. كل تغيير بصري يعيد استخدام المكونات الموجودة ولا يضيف مكتبات.
2. بعد كل مرحلة: فحص العرض الفعلي + رحلة تمثيلية + موبايل + RTL + لوحة مفاتيح + الحالات + `typecheck`/`test`/`build` + مراجعة الفرق.
3. لا أُسأل إلا إذا المسألة تغيّر هوية البراند أو السوق أو سياسة تجارية حقيقية.
