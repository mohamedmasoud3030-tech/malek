# UI_UX_MASTER_REVIEW

> مراجعة شاملة للتجربة — قائمة على المنتج المعروض فعلاً (dev server على المنفذ 5173) + الكود + الاختبارات.
> كل نتيجة تحمل: الدليل، الدور/الطريق/الحالة/حجم الشاشة، أثر المستخدم، الخطورة، التصحيح، ومعيار القبول.

## اتجاه التصميم المختار (Coherent Direction)

**«الوضوح الأخضر الموحّد»** — إبقاء هوية MALEK العربية والبنية الموجودة، مع:
1. لون براند واحد أخضر (`160 84% 27%` / `160 70% 43%`) في كل سطح بلا استثناء (شامل منع الوميض قبل التحميل).
2. نصف قطر موحّد: الكروت `rounded-xl`، الحقول `rounded-xl`، العناصر المرتفعة فقط `rounded-2xl`.
3. حلقة تركيز موحّدة `ring-4` بنسبة `10%`.
4. فعل خطأ قانوني واحد «تعذّر».
5. لا إعادة كتابة بصرية عامة — تحسينات مؤسسية آمنة قابلة للعكس.

---

## جدول النتائج

| # | النوع | الخطورة | الدليل | الأثر | التصحيح | معيار القبول |
|---|---|---|---|---|---|---|
| 1 | عيب بصري | **عالية** | `index.html` فيه ثيم منع الوميض ثابت بالأزرق `217 91% 50%` / `214 92% 62%` بينما `tokens.css` القانوني أخضر `160 84% 27%` | وميض براند أزرق على كل تحميل قبل هيدرة React — أول انطباع متقطع لكل مستخدم | مواءمة ثيم منع الوميض للأخضر (نُفّذ) | `index.html` بلا أي `217/214` أزرق، والثيم الثابت == الثيم القانوني |
| 2 | عيب بصري | عالية | انقسام براند أزرق/أخضر عبر 47+ صفحة (`tokens.css` أزرق vs `malek-pro` أخضر) | تنقّل يزحزح لون البراند ويقلق العين | توحيد `tokens.css` + `dashboard-v2.css` للأخضر (نُفّذ PR #1501) | صفر `200 85%` متبقّي في أي CSS |
| 3 | عيب بصري | متوسطة | أسطح كروت بمنصف قطر مختلط (`entity-card`/`stat-card`/`mobile-card` `rounded-2xl` vs `Card`/`kpi-card` `rounded-xl`) | حواف غير متناسقة بين الكارت والحوار | توحيد كل أسطح الكروت `rounded-xl` (نُفّذ PR #1502) | `rounded-2xl` على الطبقات المرتفعة (dialog/dropdown) فقط |
| 4 | عيب بصري | متوسطة | `Input` كان `rounded-lg`+`ring-2` بينما `Select`/`Textarea`/`DatePicker` `rounded-xl`+`ring-4` | حقول مختلطة داخل نفس النموذج | توحيد `rounded-xl`+`ring-4/10` (نُفّذ PR #1503) | كل حقل بنفس المنصف والحلقة |
| 5 | عيب محتوى | منخفضة | فعل خطأ مختلط: 28 «فشل …» (قاسٍ) مقابل 461 «تعذّر …» (ناعم) | نبرة لوم غير متسقة | توحيد «تعذّر» (نُفّذ PR #1504) | صفر «فشل …» في رسائل الخطأ المواجهة للمستخدم |
| 6 | فجوة محتوى | متوسطة | `routes/landing.tsx` يحوّل `/` → `/login`، وصفحات التسويق (`Hero`/`FeaturesBento`/`Faq`/`HowItWorks`) مفصولة عمداً | زائر أول مرة لا يرى قيمة المنتج/الجمهور/الخطوة التالية | قرار منتج: إعادة ربط landing بعد إصلاح أدائها، أو صفحة تعريف مختصرة داخل `/login` | يرى الزائر الجديد عرض القيمة قبل تسجيل الدخول |
| 7 | ملاحظة | منخفضة | `theme_color` = `#0f172a` (رمادي داكن) وليس أخضر البراند | شريط حالة المتصفح/الهاتف بلون كروم داكن — مقبول مع الـ sidebar الداكن | إبقاء (اختيار تصميم سليم) | — |
| 8 | مكتفٍ | — | `@media (prefers-reduced-motion)` موجود ويُبطل `scroll-behavior` و`transition` | — | لا شيء (تم التحقق) | — |

### ما هو سليم (تم التحقق، لا تصحيح)

- **النماذج**: محرك `EntityForm` موحّد + react-hook-form + zod، منع إرسال مزدوج، تركيز أول خطأ، حفظ المدخلات عند الفشل، شارة تغييرات غير محفوظة.
- **الحالات**: حالات فارغة صادقة («لا نعرض لا توجد تنبيهات عند فشل البيانات»)، أخطاء بخطوة تالية، حالة انقطاع في `app-shell`.
- **الوصولية**: تسميات مرتبطة، `aria-invalid`/`role=alert`، كشف Caps Lock، تباين AA موثّق، إكمال بلوحة مفاتيح.
- **RTL/موبايل**: `dir=rtl` موروث، أرقام `dir=ltr`+`tabular-nums`، حقول ≥44px، منع زوم iOS، `viewport-fit=cover` + `safe-area-inset`.
- **الثقة**: نصوص متناسبة مع الخطر، لا وعود مخترعة، لا إلحاح مصطنع، لا dark patterns.
- **نظام التصميم**: مكونات قابلة لإعادة الاستخدام (`Card`/`EntityTable`/`EmptyState`/`ConfirmDialog`...) مع عزل الأنماط المعتمدة.

---

## تصنيف النتائج

- **عيب وظيفي** (يكسر مهمة): 0 مفتوح.
- **عيب قابلية استخدام**: 0 مفتوح حرج.
- **عيب بصري**: 4 (نُفّذت كلها — #1..4).
- **عيب محتوى**: 1 (نُفّذ — #5).
- **حاجز وصولية**: 0 مفتوح.
- **حالة مفقودة متوقعة**: 0 مفتوح (الحالات مغطاة).
- **فجوة محتوى/تسويق**: 1 مفتوح (#6 — قرار منتج).
- **تفضيل شخصي**: 1 (#7 — إبقاء كما هو).
