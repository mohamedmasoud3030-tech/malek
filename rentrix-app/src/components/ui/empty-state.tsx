/**
 * Re-export EmptyState into the ui barrel so pages can import every surface
 * from `@/components/ui` without mixing top-level component paths.
 */
export { EmptyState } from '@/components/empty-state';
