/**
 * WP-C compatibility seam. The report directory moved to
 * `features/reports/directory/`. This file keeps the historical import path
 * working for routes and tests.
 */
export { ReportDirectory } from '../directory/ReportDirectory';
