# FORM_EXPERIENCE_STANDARD

> المعيار القانوني الوحيد لتجربة نماذج الإدخال في MALEK.
> كل نموذج جديد أو معدّل يجب أن يلتزم بهذا المعيار. أي انحراف يعتبر عيب تجربة مستخدم.

---

## 1. المبادئ الحاكمة (غير قابلة للتفاوض)

1. **الحفظ في الحد الموثوق (Server Boundary)**: أي كتابة مالية أو تشغيلية تمر عبر RPC/service على الخادم مع RLS. التحقق في المتصفح للتجربة فقط، وليس حماية.
2. **لا فقدان لمدخلات المستخدم**: الفشل القابل للاسترداد (شبكة/مهلة/جلسة منتهية) يحفظ المدخلات في الحالة ولا يمحوها.
3. **منع الإرسال المزدوج**: زر الإرسال يُعطّل فور بدء العملية (`isSubmitting` / `isPending`)، مع حارس `ref` إضافي في النماذج اليدوية غير المبنية على react-hook-form.
4. **أول خطأ مفيد يحصل على التركيز**: عند فشل التحقق، التركيز يذهب لأول حقل غير صالح مع رسالة موجزة بجانبه.
5. **رسائل واضحة وموجزة**: كل خطأ يذكر ما هو مطلوب بالضبط، بصيغة عربية سليمة (تشكيل الكلمات الوظيفية مثل «جارٍ» وليس «جار»).
6. **النمط الأبسط أولاً**: لا مكتبات تحقق إضافية؛ react-hook-form + zod هو القياسي الوحيد.

---

## 2. المكونات القابلة لإعادة الاستخدام (Canonical)

| المكوّن | المسار | الاستخدام |
|---|---|---|
| `EntityForm` | `components/ui/entity-form.tsx` | المحرك الوحيد للنماذج: `Root` / `Field` / `Section` / `ErrorSummary` / `Actions` / `Overlay` |
| `Input` | `components/ui/input.tsx` | حقل نصي/رقمي/بريد/تاريخ/كلمة مرور |
| `Select` | `components/ui/select.tsx` | قائمة منسدلة |
| `Textarea` | `components/ui/textarea.tsx` | نص متعدد الأسطر |
| `DatePicker` | `components/ui/date-picker.tsx` | تاريخ (native `type="date"`) |
| `SearchInput` | `components/ui/search-input.tsx` | بحث مع زر مسح |
| `FileAttachmentField` | `components/ui/file-attachment-field.tsx` | رفع مرفقات (تحقق نوع/حجم) |
| `Button` | `components/ui/button.tsx` | أزرار الإرسال/الإلغاء |

> **قاعدة**: كل حقول الإدخال تستخدم **نصف قطر موحّد `rounded-xl`** وحلقة تركيز موحّدة `focus:ring-4 focus:ring-primary/10`. لا يجوز استخدام `rounded-lg` أو `ring-2` في أي حقل.

### مكونات مهملة (لا تستخدمها في كود جديد)

| المكوّن | المسار | الحالة |
|---|---|---|
| `FormField` | `components/ui/form-field.tsx` | مهمل — استخدم `EntityForm.Field` |
| `TextField`/`TextAreaField`/`PasswordField` | `components/ui/text-field.tsx` | مهمل — استخدم `Input`/`Textarea` + `EntityForm.Field` |

> سبب الإهمال: ثلاث واجهات متداخلة لنفس الغرض. `EntityForm.Field` + `Input`/`Select`/`Textarea` هي المسار الوحيد.

---

## 3. بنية النموذج (الأنماط المعتمدة)

### 3.1 النموذج الحواري (Dialog) — الافتراضي لإنشاء/تعديل الكيانات
```tsx
<EntityForm.Overlay open={open} onOpenChange={...} title="..." description="...">
  <EntityForm.Root onSubmit={handleSubmit} aria-busy={isSubmitting}>
    <EntityForm.ErrorSummary message={submitError} />
    <EntityForm.Section title="..." description="...">
      <EntityForm.Field label="..." error={errors.x?.message}>
        <Input {...register('x')} />
      </EntityForm.Field>
    </EntityForm.Section>
    <EntityForm.Actions onCancel={onClose} isSubmitting={isSubmitting} submitLabel="حفظ" />
  </EntityForm.Root>
</EntityForm.Overlay>
```

### 3.2 التحقق (react-hook-form + zod)
- مخطط zod واحد لكل نموذج، مع رسائل عربية لكل قاعدة.
- `superRefine` للقواعد عبر الحقول (مثل: تاريخ النهاية > تاريخ البداية، نسبة العمولة ≤ 100).
- التحقق الفوري عند `submit`، وتحديث `ErrorSummary`/`Field error` من `formState.errors`.

### 3.3 قواعد الحقول الإلزامية
- الحقل الإلزامي يحمل `required` + علامة `*` بجوار التسمية.
- التسمية توضح «(اختياري)» صراحة للحقول غير الإلزامية.
- لا حقول مطلوبة في غير موضعها: الحقل يُسأل عنه فقط عند الحاجة الفعلية في الرحلة.

---

## 4. سلوك الخطأ والنجاح

| الحالة | السلوك المطلوب |
|---|---|
| خطأ تحقق | `ErrorSummary` أعلى النموذج + رسالة بجانب الحقل، والتركيز على أول حقل غير صالح |
| خطأ خادم قابل للاسترداد | رسالة في `ErrorSummary`، **المدخلات محفوظة**، زر إعادة المحاولة يعيد الإرسال |
| جلسة منتهية | إعادة توجيه لتسجيل الدخول مع رسالة، **دون فقدان المدخلات إن أمكن** |
| نجاح الحفظ | إغلاق النموذج + `toast.success` مختصر، أو رسالة `output` داخل النموذج للحالات المضمنة |
| انقطاع/مهلة | تعطيل الإرسال + رسالة «تحقق من الاتصال ثم أعد المحاولة» |

### قواعد الخطأ
- كل خطأ يحمل `role="alert"`، ورسائل الحالة `role="status"`/`aria-live="polite"`.
- الخطأ مرتبط بالحقل عبر `aria-describedby` / `aria-invalid`.
- لا رسالة خطأ مبكرة (قبل لمس الحقل) ولا متأخرة (بعد مغادرة الصفحة).

---

## 5. إمكانية الوصول (Accessibility)

1. **كل حقل له `<label>` مرتبط** بـ `htmlFor`/`id` (أو `aria-label`).
2. **إكمال بلوحة المفاتيح**: التنقل بـ Tab، وإرسال بـ Enter داخل الحقول، ولا مصيدة تركيز.
3. **قارئ الشاشة**: `aria-busy` أثناء الإرسال، `aria-invalid` للحقول الخاطئة، `role="alert"` للأخطاء.
4. **كشف Caps Lock** في حقول كلمة المرور (تحذير مرئي).
5. **زر إظهار/إخفاء كلمة المرور** بـ `aria-label` واضح.
6. **تباين AA**: نص الخطأ destructive فوق سطح فاتح ≥ 4.5:1.

---

## 6. متطلبات الموبايل و RTL

| المتطلب | القاعدة |
|---|---|
| اللمس | حقول بحد أدنى 44px (يفضل 48px) — `min-h-11` / `min-h-12` |
| لوحة المفاتيح | `inputMode` مناسب: `decimal` للرقم، `email` للبريد، `tel` للهاتف، `numeric` للأرقام |
| منع زوم iOS | خط 16px على الموبايل للحقول (`text-base sm:text-sm`) |
| تاريخ | `type="date"` + `lang="en-GB"` (صيغة موحّدة بغض النظر عن لغة الجهاز) |
| RTL | `dir="rtl"` موروث، والأرقام/العملات `dir="ltr"` بـ `tabular-nums` |
| أرقام العملات | `inputMode="decimal"` + `step` مناسب + توضيح العملة (ريال/%) |
| إغلاق لوحة المفاتيح | أزرار الإجراءات في أسفل الشاشة مع خلوص `safe-area-inset-bottom` |

---

## 7. منع المشاكل الشائعة (Checklist لكل نموذج)

- [ ] لا حقول مكررة تُسأل مرتين.
- [ ] لا حقل يُسأل مبكراً قبل الحاجة إليه في الرحلة.
- [ ] لا تعبئة تلقائية مدمرة (autofill) تحذف قيماً مقصودة.
- [ ] وحدات/عملة/منطقة زمنية واضحة (لا أرقام مبهمة بلا وحدة).
- [ ] لا إرسال مزدوج (زر معطّل + حارس ref).
- [ ] لا فقدان مدخلات عند فشل قابل للاسترداد.
- [ ] لا تحقق client-only للقيم الحساسة (المال/الصلاحيات).
- [ ] تأكيد نجاح واضح بعد الحفظ.
- [ ] حالة تحميل/إيقاف/مهلة/إلغاء/تحديث/جلسة منتهية مغطاة.
- [ ] `autoComplete` صحيح (`email`، `current-password`، `new-password`، `name`، `tel`...).

---

## 8. مصفوفة الاختبارات (Test Matrix)

| الطبقة | الملفات المرجعية | ما تغطيه |
|---|---|---|
| وحدة التحقق | `*schema*.test.ts` / `*validators*.test.ts` | قواعد zod (حدود، صيغ، قيم غير صالحة) |
| سلوك المكوّن | `entity-form.test.ts`، `property-form-core-fields.test.tsx` | رندر الحقول، ربط التسمية، حالة الخطأ |
| النموذج الكامل | `login-page.test.tsx`، `quick-payment-form.test.tsx`، `p6e-contract-form-behavior.test.tsx` | إرسال ناجح، خطأ، تعطيل مزدوج، حالة تحميل |
| عقد المتصفح | `e2e/` (Playwright) | إكمال بلوحة المفاتيح، قارئ الشاشة، موبايل، لصق، قيم حدودية |

### سيناريوهات إلزامية لكل نموذج
1. إرسال ناجح → تأكيد + إغلاق.
2. حقول فارغة → تركيز أول خطأ + رسائل.
3. قيم غير صالحة (حدود/صيغ) → رسالة دقيقة.
4. نقر مزدوج سريع → طلب واحد فقط.
5. خطأ خادم → مدخلات محفوظة + إعادة محاولة.
6. لصق نص طويل/خاص → قبول آمن أو رفض موجز.
7. بلوحة المفاتيح فقط → اكتمال الرحلة.
8. قارئ شاشة → وصول للتسميات والأخطاء.

---

## 9. التغيير المنفّذ في هذه الدورة

وحّدت **نصف القطر وحلقة التركيز** لحقول الإدخال:
- `Input`: `rounded-lg` → `rounded-xl`، و `ring-2/15` → `ring-4/10` (يطابق `Select`/`Textarea`/`SearchInput`).
- `DatePicker`: `focus-visible` → `focus` + `ring-primary/20` → `ring-primary/10` (يطابق الباقي).

النتيجة: كل حقول النموذج الواحد بحواف وحلقة تركيز متطابقة — لا اختلاط بصري داخل نفس النموذج.
