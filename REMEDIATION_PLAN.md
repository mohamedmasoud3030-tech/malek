# REMEDIATION_PLAN.md — MALEK

> **تقرير صادق عن المصادر:** المستندات المرقّمة `01_PROJECT_DISCOVERY.md`…`06_TEST_RELIABILITY_AUDIT.md`
> غير موجودة في المستودع (تحقق). يُبني هذا الدمج على الأدلة الفعلية المُتحقَّق منها:
> (1) `docs/audits/FULL_PROJECT_AUDIT_20260817_AR.md` (موجود على فرع PR #1490)؛
> (2) تدقيق الأداء/الديون التقنية (قياسات قابلة للتكرار في هذا الجلسة)؛
> (3) `TECHNICAL_REMEDIATION_PLAN.md` الحالي.
> **القاعدة:** لا بند يُدرج هنا بلا دليل قابل للتحقق في الريبو.

## ترتيب العمل حسب أولوية المخاطرة

### المجموعة 0 — أمن/اعتماد/وصول/مال/إتلاف/تعطل (قبل كل شيء)
| # | البند | الدليل | الحالة |
|---|---|---|---|
| R1 | لا أسرار في Git، `pnpm audit --prod` = 0 | فحص grep + audit | VERIFIED — لا إصلاح مطلوب |
| R2 | **SonarCloud معطّل** (يعمل يدوياً فقط) | `workflow: if: workflow_dispatch` → skipping على كل PR | معالجة في PR #1491 (مفتوح) — قرار مالك |
| R3 | 22 ثغرة dev-toolchain (vite/vitest...) | `pnpm audit` كامل | تُعالج بترقية vite+vitest معاً (Milestone B) |

### المجموعة 1 — رحلات أساسية معطوبة / عوائق build
| # | البند | الدليل | الحالة |
|---|---|---|---|
| C1 | `sitemap.xml` قديم: روابط نسبية + `/finance` غير موجود + مسارات محمية | `public/sitemap.xml` مقابل `route-tree.ts` | **VERIFIED COMPLETE** — روابط مطلقة للصفحات العامة فقط |
| C2 | بناء إنتاج = PASS (12 ثانية)، db0 = 7/7 | قياس محلي | VERIFIED — سليم |

### المجموعة 2 — صحة البيانات/المصادقة/التكامل
| # | البند | الدليل | الحالة |
|---|---|---|---|
| D1 | `select('*')` في 76 موضعاً + ترقيم جزئي | فحص grep | تُعالج لكل صفحة (Milestone B/C) |
| D2 | 14 باقة معلنة وغير مستخدمة | فحص grep (0 استيراد) | **VERIFIED COMPLETE** — حُذفت من package.json + lockfile |

### المجموعة 3 — حماية الانحدار
| # | البند | الدليل | الحالة |
|---|---|---|---|
| T1 | فجوة: لا اختبار يقفل `sitemap.xml` ولا حجم حزمة الإدخال | فحص | **VERIFIED COMPLETE** — `sitemap-contract.test.ts` (3 اختبارات PASS) |

### المجموعة 4 — نطاق/PWA/نشر/UX/أداء
| # | البند | الدليل | الحالة |
|---|---|---|---|
| P1 | فيديو demo 19MB على الصفحة الهبوطية | `public/landing/rentrix-demo.mp4` | **قرار مالك** (أصول تسويقية) — خارج النطاق الآمن |
| P2 | حزمة إدخال 642kB/190gzip | قياس curl + build | تُقاس قبل أي manualChunks (قرار مالك) |
| P3 | عدم استخدام `react-icons` مقابل `lucide-react` | فحص (0 استيراد) | أُزيل مع D2 |

### المجموعة 5 — ديون تقنية منخفضة القيمة
| # | البند | الدليل | الحالة |
|---|---|---|---|
| E1 | الترحيلات الكبيرة (>1500 سطر: 4 ملفات) | wc | لا تلمس — المخطط مجمّد عبر db0 |
| E2 | `any` casts (107) | grep | تُعالج عند تجميد المخطط الحيّ |

## الحالة الكلية (مُحدَّثة بعد Milestone A)
- **Milestone A:** ✅ مكتمل — sitemap + حذف 14 باقة + اختبار sitemap. الفحوص: typecheck PASS، build PASS (12.6s)، 648 اختبار PASS، audit --prod = 0.
- **ملاحظة:** ~19 حزمة radix أخرى في package.json بلا استيراد مباشر — تُركت لأنها قد تكون تبعيات تلافيائية (sonner/radix-toast)؛ تَتطلب تحققاً مع `pnpm install`+build قبل حذفها.

## الحالة الكلية
- **Milestone A (أمان/فوري):** إصلاح sitemap + حذف الباقات الميتة + إضافة اختبار يثبّت sitemap. — **جاهز للتنفيذ الآمن الآن.**
- **Milestone B (مراجعة):** ترقية vite/vitest محكوماً + تقليم select('*') للصفحات الأكثر زيارة.
- **Milestone C (يتطلب مالك):** فيديو demo، manualChunks، SonarCloud.
- **خارج النطاق:** أي إعادة كتابة إطار/معمارية/ترحيلات.
