# MALEK — AI Decisions Log

> Non-canonical working decisions for this agent session. Does not override Canonical Pack or governance ledgers.

## Decision policy used

1. Prefer canonical rules and locked D01–D18 over local taste.
2. Prefer smallest reversible fix inside existing architecture.
3. Do not grant governed stage credit.
4. Do not invent legal/tax/accounting policy.
5. Ask owner only for credentials, paid services, production mutation, or market/pricing/legal commitments.

## D-AI-001 — Assessment baseline

- **Decision:** Treat Git branch tip + Canonical Pack + local inspection as baseline; historical audits supporting only.
- **Reversible:** yes.

## D-AI-002..D-AI-009 — Prior session decisions

PWA PNG icons, robots absolute sitemap, placeholder env login honesty, local secret handling, company selector MSA copy, visibility maybeSingle pack, hot-path index migration, QA apply pack when egress blocked. See git history on this branch.

## D-AI-010 — Milestone E priority: bank reconciliation error honesty

- **Decision:** Next autonomous milestone after visibility/index work is **fail-closed empty vs error** on bank reconciliation (and payment-terms settings), not Master Lease or visual redesign.
- **Why:** Confirmed code path shows empty list + zero KPIs when queries fail — high trust damage for money operators. Fully local, reversible, no schema/credentials.
- **Rejected:** Full UI redesign; inventing hosted journey pass without network; bulk-changing every EmptyState that intentionally styles errors with `role=alert` (expenses already document error intent).
- **Reversible:** yes.

## D-AI-011 — Hide KPI grid on read failure

- **Decision:** Do not render bank reconciliation KPI cards while accounts or lines queries are in error.
- **Why:** `summary` is derived from `linesQuery.data ?? []`, so failures become zeros.
- **Reversible:** yes.

## Open decisions requiring owner

| Topic | Why blocked |
|---|---|
| Finish QA index apply via SQL Editor | Sandbox TLS egress blocked; pack at `evidence/qa-index-apply/` |
| Confirm `hot_path_indexes_present = 24` | Owner/operator after SQL apply |
| Hosted Auth Hook / RLS live proof | External |
| Production migration ledger reconcile + backup | External / HOLD |
| SonarCloud always-on / paid monitoring | Cost |
| S08 accounting sign-off | Professional |
