# INFORMATION_ARCHITECTURE

> الوثيقة القانونية الوحيدة لبنية المعلومات والتنقل في MALEK.
> أي طريق أو تسمية أو مدخل تنقّل جديد يلتزم بهذه الوثيقة.

---

## 1. النموذج المختار

**التنقّل المتمحور حول المهمة (Task-centric IA) بسبعة جذور** — من يبدأ بـ«ما الذي أحتاج عمله الآن؟» ثم يتفرّع إلى سياق العمل.

```
اليوم  →  المحفظة  →  التأجير  →  المال  →  الخدمات  →  التقارير والكشوف  →  الإعدادات
```

- **المجموعة 1 «العمل»**: اليوم، المحفظة، التأجير، المال، الخدمات.
- **المجموعة 2 «التحليل والإدارة»**: التقارير والكشوف، الإعدادات.

**السبب**: تفضيل وجهات واضحة قليلة (7) على تعريض كل ميزة بالتساوي. سجلات الكيانات (الملاك، الوحدات، الأراضي، المستأجرون، جهات التعامل...) لا تنافس كمنتجات مستقلة في الـ sidebar، بل تُكشف تدريجيًا داخل مساحة العمل التي تملك مهمة المستخدم.

---

## 2. خريطة الطرق والأدوار

| الجذر | الطريق القانوني | الأدوار/الصلاحية | الأطفال (progressive disclosure) |
|---|---|---|---|
| اليوم | `/dashboard` | كل المعتمدين | — |
| المحفظة | `/properties` | كل المعتمدين | الوحدات، الأراضي (`lands.view`)، الملاك (`owners.hub.view`) |
| التأجير | `/contracts` | كل المعتمدين | المستأجرون، جهات التعامل، العملاء المحتملون (`leads.view`)، التواصل (`communication.view`) |
| المال | `/financials` | كل المعتمدين (تتفرّع بالصلاحية) | الفواتير، الإيصالات، المتأخرات، المصروفات، التأمينات، تسويات الملاك، المطابقة البنكية، العمولات |
| الخدمات | `/maintenance` | كل المعتمدين | الصيانة، مزودو الخدمات، المرافق، خزينة المستندات |
| التقارير والكشوف | `/reports` | كل المعتمدين | المحاسبة (دفتر الأستاذ العام) |
| الإعدادات | `/settings` | إدارة | الشركة، المستخدمون والصلاحيات، مراكز التكلفة، الأتمتة، إعدادات النظام |

- **المساعد الذكي** `/ai-assistant`: أداة قراءة-فقط، لا تظهر كجذر (تصل عبر بحث/أيقونة).
- **التغيير/التدقيق/السلامة/النظام**: داخل الإعدادات بالصلاحية المناسبة (`audit.view`, `integrity.view`, `system.view`).

**قاعدة الصلاحية**: إخفاء عنصر التنقّل ليس تفويضًا أبدًا — الحماية الحقيقية على الخادم (RLS/RPC). التنقّل يخفيه لتقليل الضوضاء فقط.

---

## 3. مصدر الحقيقة الوحيد (Single Source of Truth)

| الملف | الدور |
|---|---|
| `app/navigation/route-contract.ts` | يربط: الطريق القانوني → الأسماء القديمة → الجذر → الصلاحية → تنقّل الموبايل |
| `app/navigation/app-nav-items.ts` | بنية الـ sidebar الفعلية (7 جذور + أطفال مساحات العمل) |
| `app/navigation/terminology-registry.ts` | **القاموس الوحيد للتسمية المرئية** (`navigationLabels` + `workspaceLabels`) |
| `app/navigation/route-nav-map.ts` | `getNavRoot(path)` — يحدد الحالة النشطة لأي طريق |

> **تحذير سابق**: كانت هناك خريطتا تسمية يتيمة (`hubPageTitles` و `canonicalTerms`) تناقضان التسمية النشطة (مثل «التشغيل والصيانة» مقابل «الخدمات»، و«لوحة التحكم» مقابل «اليوم»). **أُزيلتا** (صفر مستهلكين) لإبقاء مصدر تسمية واحد.

---

## 4. قواعد التسمية (Naming Glossary)

| الجذر | تسمية الـ sidebar (النشطة) | الطريق (متوافق) |
|---|---|---|
| اليوم | **اليوم** | `/dashboard` |
| المحفظة | **المحفظة** | `/properties` |
| التأجير | **التأجير** | `/contracts` |
| المال | **المال** | `/financials` |
| الخدمات | **الخدمات** | `/maintenance` |
| التقارير | **التقارير والكشوف** | `/reports` |
| الإعدادات | **الإعدادات** | `/settings` |

> تسمية الـ sidebar **تطابق ترويسة صفحة الـ hub** (المال/التأجير/المحفظة/الخدمات) — لا انفصال بين «ما ضغطت عليه» و«أين أنا».

مصطلحات الكيانات (من `workspaceLabels`): العقارات، الوحدات، الأراضي، الملاك، المستأجرون، جهات التعامل، العملاء المحتملون، مزودو الخدمات، المرافق، خزينة المستندات، الفواتير، الإيصالات، المصروفات، المتأخرات، التأمينات، تسويات الملاك، المطابقة البنكية، العمولات.

---

## 5. قواعد التنقّل

1. **لا طريق قديم يعيد 404** — كل اسم قديم يتحوّل (redirect) مع الحفاظ على `?search`.
2. **كل طريق محمي يعرض حالة** (تحميل/خطأ/فارغ) — لا صفحة بيضاء.
3. **الحالة النشطة تُحل لكل طريق مسجّل** (`getNavRoot`).
4. **صلاحية عنصر التنقّل == `requirePermission()` في شجرة الطرق** (تطابق إلزامي).
5. **الموبايل = قائمة + بحث فقط** (لا شريط 5 تبويبات) — قرار مقفول.
6. **الروابط العميقة محفوظة** — `/invoices`، `/owners/$id`... تبقى صالحة وتلزم داخل الـ hub.
7. **الرجوع/التحديث/الرابط العميق** يعمل عبر TanStack Router — الحالة تُحفظ في query params (`?section&view`).

---

## 6. اعتبارات الهجرة والتحويلات

المسارات القديمة التي تتحوّل (تُبقى للروابط المحفوظة والإشارات القديمة):

`/landing`، `/units`، `/utilities`، `/automation`، `/documents-vault`، `/finance/collections`، `/finance/expenses`، `/finance/deposits`، `/finance/banking`، `/expenses`، `/invoices`، `/receipts`، `/arrears`، `/deposits`، `/owner-settlements`، `/bank-reconciliation`، `/accounting`، `/change-password`، `/audit-log`، `/data-integrity`، `/system`.

> لا تُحذف التحويلات حتى يثبت صفر زيارات عبر التحليلات وتبقى إصدارًا كاملاً إضافيًا.

---

## 7. اختبارات القبول

| الاختبار | ما يضمنه |
|---|---|
| `route-contract.test.ts` | ربط الطرق بالأسماء القديمة والجذور والصلاحية |
| `app-nav-items.test.ts` | الـ 7 جذور بترتيبها وتسميتها القانونية |
| `phase2-canonical-ia.test.ts` | كل جذر يحتوي مساحاته الصحيحة، لا ازدواج |
| `permission-visibility.test.ts` | إخفاء التنقّل حسب الدور بدون كونه تفويضًا |
| `navigation-active-state.test.ts` | الحالة النشطة تحُل لكل طريق |
| `legacy-compatibility.test.ts` | لا 404 للطرق القديمة |
| `route-nav-map.test.ts` | `getNavRoot` صحيح + المصطلحات القانونية |

---

## 8. التغيير المنفّذ في هذه الدورة

أزلت خريطتي التسمية اليتيمتين (`hubPageTitles` و `canonicalTerms`) من `terminology-registry.ts` — صفر مستهلكين، وكانتا تناقضان التسمية النشطة (خلقا غموضًا في التسمية). النتيجة: مصدر تسمية مرئي واحد (`navigationLabels` + `workspaceLabels`) يطابق ترويسات الـ hubs الفعلية.

**ما لم يتغير**: الطرق، السلوك، الصلاحيات، البيانات، النطاق.
